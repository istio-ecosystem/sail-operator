---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: tracingintegrations.sailoperator.io
spec:
  group: sailoperator.io
  names:
    categories:
    - istio-io
    kind: TracingIntegration
    listKind: TracingIntegrationList
    plural: tracingintegrations
    singular: tracingintegration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The tracing integration type.
      jsonPath: .spec.type
      name: Type
      type: string
    - description: Whether the tracing integration is reconciled.
      jsonPath: .status.conditions[?(@.type=="Reconciled")].status
      name: Ready
      type: string
    - description: The current state of this object.
      jsonPath: .status.state
      name: Status
      type: string
    - description: The age of the object
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TracingIntegration configures Istio tracing integrations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TracingIntegrationSpec defines the desired state of TracingIntegration.
            properties:
              openTelemetry:
                description: OpenTelemetry configures integration with an OpenTelemetry
                  Collector.
                properties:
                  otelCollectorRef:
                    description: OTELCollectorRef is a reference to an OpenTelemetry
                      Collector resource.
                    properties:
                      name:
                        description: Name is the name of the referenced resource.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is the namespace of the referenced
                          resource.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                required:
                - otelCollectorRef
                type: object
              targetRefs:
                description: TargetRefs specifies the resources that this integration
                  configures.
                items:
                  description: TargetReference identifies a resource that the integration
                    configures.
                  properties:
                    kind:
                      description: Kind specifies the kind of resource (e.g. "Istio",
                        "Kiali").
                      maxLength: 63
                      minLength: 1
                      type: string
                    name:
                      description: Name is the name of the target resource.
                      minLength: 1
                      type: string
                    namespace:
                      description: |-
                        Namespace is the namespace of the target resource.
                        Only required for namespace-scoped resources like Kiali.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                maxItems: 10
                minItems: 1
                type: array
                x-kubernetes-validations:
                - message: targetRefs must not contain multiple targets of the same
                    kind
                  rule: self.all(ref, self.filter(other, other.kind == ref.kind).size()
                    == 1)
              telemetryName:
                default: mesh-default
                description: TelemetryName specifies the name of the Istio Telemetry
                  resource managed by this integration.
                minLength: 1
                type: string
              type:
                description: Type specifies the tracing integration type.
                enum:
                - OpenTelemetry
                type: string
            required:
            - targetRefs
            - type
            type: object
          status:
            description: TracingIntegrationStatus defines the observed state of TracingIntegration.
            properties:
              conditions:
                description: Represents the latest available observations of the object's
                  current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed for this
                  TracingIntegration object.
                format: int64
                type: integer
              state:
                description: Reports the current state of the object.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.openTelemetry must be set if and only if spec.type is OpenTelemetry
          rule: 'self.spec.type == ''OpenTelemetry'' ? has(self.spec.openTelemetry)
            : !has(self.spec.openTelemetry)'
    served: true
    storage: true
    subresources:
      status: {}
